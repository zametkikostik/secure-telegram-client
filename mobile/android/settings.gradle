rootProject.name = 'SecureMessenger'

// Include React Native Gradle Plugin (from root node_modules via workspaces)
def rnGradlePlugin = file('../../node_modules/@react-native/gradle-plugin')
if (rnGradlePlugin.exists()) {
    includeBuild(rnGradlePlugin)
}

include ':app'

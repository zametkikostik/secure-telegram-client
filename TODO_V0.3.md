# 📋 Техническое задание на разработку v0.3.0

**Версия**: 0.3.0  
**Этап**: В разработке (Q2 2026)  
**Исполнитель**: OpenClaw / AI-ассистент / Разработчик

---

## 🎯 Цель версии v0.3.0

Улучшение пользовательского опыта и расширение функциональности:
- GUI для desktop-версии
- Mesh-сеть для офлайн-коммуникации
- Улучшенная синхронизация
- Расширенная стеганография

---

## 📦 Задачи для реализации

### 1️⃣ 🟡 GUI клиент (Tauri)

**Приоритет**: 🔴 Высокий  
**Сложность**: ⭐⭐⭐⭐ (Высокая)  
**Время**: ~2-3 недели

#### Описание:
Создание графического интерфейса для desktop-версии на базе Tauri (Rust + Web).

#### Требования:
- **Фреймворк**: Tauri v2.x
- **Frontend**: React/Vue.js + TypeScript
- **Backend**: Rust (существующий core)
- **Платформы**: Linux, Windows, macOS

#### Функционал:
```
├── Главное окно
│   ├── Список чатов
│   ├── Активный чат
│   └── Боковая панель (настройки, контакты)
├── Окно настроек
│   ├── Настройки аккаунта
│   ├── Настройки безопасности
│   ├── Настройки транспорта
│   └── Настройки приватности
├── Окно обновлений
│   ├── Проверка обновлений
│   ├── История версий
│   └── Установка обновлений
└── Системные уведомления
    ├── Новые сообщения
    ├── Статус соединения
    └── Предупреждения безопасности
```

#### Технические детали:
```rust
// Интеграция с существующим core
use secure_messenger_core::*;

#[tauri::command]
async fn send_message(
    state: State<'_, AppState>,
    recipient: String,
    content: String,
) -> Result<MessageId, String> {
    state.client.send_message(recipient, content).await
        .map_err(|e| e.to_string())
}

#[tauri::command]
async fn receive_messages(
    state: State<'_, AppState>,
) -> Result<Vec<Message>, String> {
    state.client.receive_messages().await
        .map_err(|e| e.to_string())
}
```

#### Структура проекта:
```
secure-telegram-client/
├── src/                    # Существующий core
├── desktop-gui/            # Новый Tauri проект
│   ├── src/
│   │   ├── main.rs        # Tauri setup
│   │   ├── commands.rs    # Tauri commands
│   │   └── state.rs       # App state
│   ├── ui/                 # Frontend
│   │   ├── src/
│   │   │   ├── App.tsx
│   │   │   ├── components/
│   │   │   └── pages/
│   │   ├── package.json
│   │   └── vite.config.ts
│   ├── tauri.conf.json
│   └── Cargo.toml
└── ...
```

#### Критерии приёмки:
- [ ] Сборка работает на Linux
- [ ] Сборка работает на Windows
- [ ] Сборка работает на macOS
- [ ] Интеграция с core (отправка/получение сообщений)
- [ ] Настройки сохраняются
- [ ] Уведомления работают
- [ ] Размер дистрибутива < 50 MB

---

### 2️⃣ 🟡 Mesh режим (Bluetooth/Wi-Fi Direct)

**Приоритет**: 🟡 Средний  
**Сложность**: ⭐⭐⭐⭐⭐ (Очень высокая)  
**Время**: ~3-4 недели

#### Описание:
P2P коммуникация без интернета через Bluetooth и Wi-Fi Direct.

#### Требования:
- **Bluetooth LE**: Обнаружение и соединение
- **Wi-Fi Direct**: Прямое соединение между устройствами
- **Шифрование**: End-to-end (существующее)
- **Маршрутизация**: Mesh-сеть (несколько прыжков)

#### Функционал:
```
├── Обнаружение устройств
│   ├── Bluetooth scan
│   ├── Wi-Fi Direct scan
│   └── Отображение доступных пиров
├── Соединение
│   ├── Pairing (Bluetooth)
│   ├── Wi-Fi Direct negotiation
│   └── Key exchange
├── Передача сообщений
│   ├── Direct (1 hop)
│   ├── Multi-hop (mesh routing)
│   └── Store-and-forward
└── Управление сетью
    ├── Список пиров
    ├── Статус соединений
    └── Настройки видимости
```

#### Технические детали:
```rust
// Bluetooth LE
use btleplug::api::{Central, Manager, Peripheral};
use btleplug::platform::Adapter;

async fn scan_bluetooth_devices(adapter: &Adapter) -> Vec<Device> {
    adapter.start_scan(ScanFilter::default()).await?;
    adapter.event_receiver().filter_map(|e| {
        match e {
            CentralEvent::DeviceDiscovered(device) => Some(device),
            _ => None,
        }
    }).collect().await
}

// Wi-Fi Direct
use p2p::wifi_direct::WifiDirectManager;

struct MeshTransport {
    bluetooth: BluetoothTransport,
    wifi_direct: WifiDirectTransport,
}

impl MeshTransport {
    async fn send_to_peer(&self, peer: &PeerId, data: &[u8]) -> Result<()> {
        // Выбрать лучший транспорт
        if self.wifi_direct.is_available(peer) {
            self.wifi_direct.send(peer, data).await
        } else if self.bluetooth.is_available(peer) {
            self.bluetooth.send(peer, data).await
        } else {
            Err(Error::NoRouteToPeer)
        }
    }
}
```

#### Зависимости:
```toml
[dependencies]
# Bluetooth
btleplug = "0.11"
bluez-async = "0.7"

# Wi-Fi Direct
p2p-rs = "0.3"
tokio = { version = "1", features = ["full"] }

# Mesh routing
routing-rs = "0.2"
```

#### Критерии приёмки:
- [ ] Обнаружение устройств по Bluetooth
- [ ] Обнаружение устройств по Wi-Fi Direct
- [ ] Соединение между 2 устройствами
- [ ] Передача сообщений (до 1 KB)
- [ ] Multi-hop маршрутизация (3+ прыжка)
- [ ] Шифрование сообщений
- [ ] Работа без интернета

---

### 3️⃣ 🟡 Улучшенная синхронизация между устройствами

**Приоритет**: 🟡 Средний  
**Сложность**: ⭐⭐⭐ (Средняя)  
**Время**: ~2 недели

#### Описание:
Синхронизация сообщений и настроек между несколькими устройствами пользователя.

#### Требования:
- **Протокол**: Matrix / Custom P2P
- **Шифрование**: End-to-end
- **Конфликты**: Last-write-wins + CRDT
- **Транспорт**: Internet + Mesh fallback

#### Функционал:
```
├── Синхронизация сообщений
│   ├── Отправленные сообщения
│   ├── Полученные сообщения
│   └── Статусы (прочитано/доставлено)
├── Синхронизация настроек
│   ├── Конфигурация аккаунта
│   ├── Настройки безопасности
│   └── Списки контактов
├── Разрешение конфликтов
│   ├── Векторные часы
│   ├── CRDT для списков
│   └── Last-write-wins для значений
└── Управление устройствами
    ├── Список доверенных устройств
    ├── Добавление устройства
    └── Отозыв доступа
```

#### Технические детали:
```rust
// CRDT для синхронизации
use crdt::{GSet, LWWReg, VectorClock};

struct SyncState {
    // Last-Write-Wins для простых значений
    settings: LWWReg<Config, VectorClock>,
    
    // G-Set для растущих коллекций
    contacts: GSet<Contact, VectorClock>,
    
    // Сообщение с метаданными
    messages: Vec<LWWReg<Message, VectorClock>>,
}

impl SyncState {
    fn merge(&mut self, other: Self) {
        self.settings.merge(other.settings);
        self.contacts.merge(other.contacts);
        
        // Merge messages
        for msg in other.messages {
            self.messages.push(msg);
        }
        self.messages.sort_by_key(|m| m.timestamp);
    }
}

// Протокол синхронизации
async fn sync_with_device(
    local: &mut SyncState,
    remote: &DeviceId,
) -> Result<()> {
    // 1. Обмен векторными часами
    let remote_clock = fetch_remote_clock(remote).await?;
    
    // 2. Вычислить разницу
    let delta = local.compute_delta(&remote_clock);
    
    // 3. Передать изменения
    send_delta(remote, &delta).await?;
    
    // 4. Получить изменения
    let remote_delta = receive_delta(remote).await?;
    
    // 5. Применить и merge
    local.apply_delta(remote_delta);
    
    Ok(())
}
```

#### Критерии приёмки:
- [ ] Синхронизация между 2 устройствами
- [ ] Синхронизация между 3+ устройствами
- [ ] Разрешение конфликтов (CRDT)
- [ ] Шифрование при синхронизации
- [ ] Инкрементальная синхронизация
- [ ] Отмена/повтор при ошибках

---

### 4️⃣ 🟡 Расширенная стеганография

**Приоритет**: 🟢 Низкий  
**Сложность**: ⭐⭐⭐ (Средняя)  
**Время**: ~1-2 недели

#### Описание:
Улучшение существующей стеганографии (LSB) с поддержкой дополнительных форматов.

#### Требования:
- **Изображения**: PNG, JPEG, WebP
- **Аудио**: WAV, MP3 (LSB в частотах)
- **Видео**: MP4 (кадры + аудио)
- **Устойчивость**: К сжатию, кропу

#### Функционал:
```
├── Стеганография изображений
│   ├── LSB (существующее)
│   ├── DCT (для JPEG)
│   └── Адаптивное внедрение
├── Стеганография аудио
│   ├── LSB в PCM (WAV)
│   ├── DCT в MP3
│   └── Эхо-скрытие
├── Стеганография видео
│   ├── Внедрение в кадры
│   └── Внедрение в аудио-дорожку
└── Управление
    ├── Выбор формата
    ├── Настройка ёмкости
    └── Проверка целостности
```

#### Технические детали:
```rust
// DCT стеганография для JPEG
use image::{DynamicImage, ImageFormat};
use dct::dct_2d;

pub struct DctSteganography {
    quality: u8,
}

impl DctSteganography {
    pub fn embed(&self, image: &DynamicImage, data: &[u8]) -> Result<DynamicImage> {
        // 1. Конвертировать в YCbCr
        let ycbcr = image.to_ycbcr();
        
        // 2. Применить DCT к Cb/Cr каналам
        let mut cb_dct = dct_2d(&ycbcr.cb);
        let mut cr_dct = dct_2d(&ycbcr.cr);
        
        // 3. Внедрить данные в средние частоты
        self.embed_in_dct(&mut cb_dct, &mut cr_dct, data);
        
        // 4. Обратное DCT
        let cb = idct_2d(&cb_dct);
        let cr = idct_2d(&cr_dct);
        
        // 5. Конвертировать обратно в RGB
        Ok(DynamicImage::from_ycbcr(ycbcr.y, cb, cr))
    }
    
    fn embed_in_dct(&self, cb: &mut [f32], cr: &mut [f32], data: &[u8]) {
        // Внедрение в средние частоты
        let mut bit_index = 0;
        for (i, coeff) in cb.iter_mut().enumerate() {
            if i > 10 && i < 100 { // Средние частоты
                if bit_index < data.len() * 8 {
                    let bit = (data[bit_index / 8] >> (bit_index % 8)) & 1;
                    *coeff = if bit == 1 {
                        coeff.abs().max(10.0)
                    } else {
                        coeff.abs().min(5.0) * -1.0
                    };
                    bit_index += 1;
                }
            }
        }
    }
}

// Аудио стеганография
use hound::{WavReader, WavWriter, SampleFormat};

pub struct AudioSteganography;

impl AudioSteganography {
    pub fn embed_wav(&self, wav_path: &str, data: &[u8], output: &str) -> Result<()> {
        let reader = WavReader::open(wav_path)?;
        let spec = reader.spec();
        let samples: Vec<i16> = reader.samples::<i16>().collect::<Result<_, _>>()?;
        
        let mut writer = WavWriter::create(output, spec)?;
        
        // LSB внедрение в младшие биты
        for (i, sample) in samples.iter().enumerate() {
            if i < data.len() * 8 {
                let bit = (data[i / 8] >> (i % 8)) & 1;
                let modified = (sample & !1) | bit as i16;
                writer.write_sample(modified)?;
            } else {
                writer.write_sample(*sample)?;
            }
        }
        
        writer.finalize()?;
        Ok(())
    }
}
```

#### Критерии приёмки:
- [ ] LSB для PNG (существующее)
- [ ] DCT для JPEG (устойчивость к сжатию)
- [ ] LSB для WAV
- [ ] Внедрение в MP3
- [ ] Ёмкость: 10% от размера файла
- [ ] Извлечение без ошибок
- [ ] Детектирование повреждений

---

## 📊 План спринтов

### Спринт 1 (Недели 1-2):
- [ ] Настройка Tauri проекта
- [ ] Базовый UI (список чатов, настройки)
- [ ] Интеграция с core (отправка/получение)

### Спринт 2 (Недели 3-4):
- [ ] Bluetooth LE обнаружение
- [ ] Wi-Fi Direct соединение
- [ ] Базовая mesh-маршрутизация

### Спринт 3 (Недели 5-6):
- [ ] CRDT синхронизация
- [ ] Векторные часы
- [ ] Multi-device тесты

### Спринт 4 (Недели 7-8):
- [ ] DCT стеганография
- [ ] Аудио стеганография
- [ ] Интеграция всех компонентов
- [ ] Тестирование и багфикс

---

## 🧪 Тестирование

### Unit тесты:
```bash
# GUI тесты
cd desktop-gui && cargo test

# Mesh тесты
cargo test --package mesh_transport

# Sync тесты
cargo test --package sync_engine

# Stego тесты
cargo test --package steganography
```

### Integration тесты:
```bash
# GUI + Core
cargo test --test gui_integration

# Mesh сеть (2+ устройства)
cargo test --test mesh_network

# Синхронизация (3+ устройства)
cargo test --test multi_device_sync
```

### Ручное тестирование:
- [ ] GUI на Linux
- [ ] GUI на Windows
- [ ] GUI на macOS
- [ ] Bluetooth (5+ метров)
- [ ] Wi-Fi Direct (10+ метров)
- [ ] Mesh (3+ прыжка)
- [ ] Синхронизация (2+ устройства)
- [ ] Stego (PNG, JPEG, WAV, MP3)

---

## 📚 Документация

### Создать:
- `docs/TAURI_SETUP.md` — Настройка Tauri
- `docs/MESH_PROTOCOL.md` — Mesh протокол
- `docs/SYNC_PROTOCOL.md` — Протокол синхронизации
- `docs/STEGANOGRAPHY.md` — Стеганография методы

### Обновить:
- `README.md` — Добавить информацию о v0.3.0
- `QUICKSTART.md` — GUI инструкции
- `CHANGELOG.md` — История изменений v0.3.0

---

## ✅ Чеклист релиза v0.3.0

- [ ] GUI клиент работает (Linux/Windows/macOS)
- [ ] Mesh режим (Bluetooth + Wi-Fi Direct)
- [ ] Синхронизация между устройствами
- [ ] Расширенная стеганография
- [ ] Все тесты проходят
- [ ] Документация обновлена
- [ ] APK обновлён (Android Mesh)
- [ ] Релиз на GitHub
- [ ] Отправка в F-Droid
- [ ] Анонс в социальных сетях

---

**Исполнитель**: OpenClaw / AI-ассистент  
**Срок**: Q2 2026 (Апрель-Июнь)  
**Статус**: 🟡 В разработке

# Руководство для участников (Contributing Guidelines)

Благодарим за интерес к проекту Secure Telegram Client! Это руководство поможет вам начать участие в разработке.

## 📋 Содержание

- [Кодекс поведения](#кодекс-поведения)
- [Как внести вклад](#как-внести-вклад)
- [Настройка окружения](#настройка-окружения)
- [Стандарты кода](#стандарты-кода)
- [Pull Request процесс](#pull-request-процесс)
- [Стиль коммитов](#стиль-коммитов)

## Кодекс поведения

- Будьте уважительны к другим участникам
- Конструктивная критика приветствуется
- Фокусируйтесь на улучшении проекта

## Как внести вклад

### Сообщение об ошибке

1. Проверьте существующие issues
2. Опишите проблему подробно
3. Укажите шаги для воспроизведения
4. Приложите логи и версию ПО

### Предложение функции

1. Создайте issue с описанием функции
2. Объясните, зачем это нужно
3. Обсудите реализацию с мейнтейнерами

### Код

1. Форкните репозиторий
2. Создайте ветку (`git checkout -b feature/amazing-feature`)
3. Внесите изменения
4. Протестируйте
5. Создайте Pull Request

## Настройка окружения

### Требования

- Rust 1.75+
- CMake 3.10+
- Clang
- OpenSSL dev пакеты
- TDLib 2.0+

### Установка зависимостей

**Ubuntu/Debian:**
```bash
sudo apt-get update
sudo apt-get install -y cmake clang libssl-dev pkg-config git
```

**macOS:**
```bash
brew install cmake openssl
```

**Windows:**
```powershell
vcpkg install openssl:x64-windows
```

### Сборка проекта

```bash
# Клонируйте репозиторий
git clone https://github.com/YOUR_USERNAME/secure-telegram-client.git
cd secure-telegram-client

# Сборка debug версии
cargo build

# Сборка release версии
cargo build --release

# Запуск тестов
cargo test

# Проверка кода
cargo clippy -- -D warnings

# Форматирование
cargo fmt
```

## Стандарты кода

### Rust стиль

Следуйте [Rust API Guidelines](https://rust-lang.github.io/api-guidelines/):

- Используйте `snake_case` для функций и переменных
- Используйте `PascalCase` для типов и структур
- Используйте `SCREAMING_SNAKE_CASE` для констант
- Избегайте `unwrap()`, используйте обработку ошибок

### Обработка ошибок

```rust
// ✅ Хорошо
fn load_config() -> Result<Config> {
    let content = fs::read_to_string(path)
        .with_context(|| format!("Failed to read config: {:?}", path))?;
    Ok(serde_json::from_str(&content)?)
}

// ❌ Плохо
fn load_config() -> Config {
    let content = fs::read_to_string(path).unwrap();
    serde_json::from_str(&content).unwrap()
}
```

### Документирование

```rust
/// Краткое описание функции
///
/// Подробное описание с примерами использования.
///
/// # Arguments
///
/// * `param1` - Описание параметра
///
/// # Returns
///
/// Описание возвращаемого значения
///
/// # Errors
///
/// Описание возможных ошибок
///
/// # Example
///
/// ```
/// let result = function_name(param1);
/// ```
pub fn function_name(param1: Type) -> Result<ReturnType> {
    // ...
}
```

## Pull Request процесс

### Перед созданием PR

1. Убедитесь, что код проходит все тесты:
   ```bash
   cargo test --all
   ```

2. Проверьте код на предупреждения:
   ```bash
   cargo clippy -- -D warnings
   ```

3. Отформатируйте код:
   ```bash
   cargo fmt
   ```

4. Обновите документацию если нужно

### Шаблон PR

```markdown
## Описание
Краткое описание изменений

## Тип изменений
- [ ] Багфикс
- [ ] Новая функция
- [ ] Улучшение документации
- [ ] Рефакторинг

## Чеклист
- [ ] Код отформатирован (cargo fmt)
- [ ] Нет предупреждений (cargo clippy)
- [ ] Тесты проходят (cargo test)
- [ ] Документация обновлена

## Тестирование
Опишите, как вы тестировали изменения

## Скриншоты (если применимо)
```

## Стиль коммитов

Следуйте [Conventional Commits](https://www.conventionalcommits.org/):

```
<type>(<scope>): <description>

[optional body]

[optional footer]
```

### Типы коммитов

- `feat`: Новая функция
- `fix`: Исправление ошибки
- `docs`: Изменения в документации
- `style`: Форматирование, отступы
- `refactor`: Рефакторинг кода
- `perf`: Улучшение производительности
- `test`: Добавление тестов
- `chore`: Изменения в сборке, зависимостях

### Примеры

```
feat(crypto): добавить поддержку Kyber-1024

fix(tdlib): исправить обработку ошибок авторизации

docs(readme): обновить инструкцию по установке

refactor(cli): упростить парсинг аргументов

test(stego): добавить интеграционные тесты для LSB
```

## Запуск бенчмарков

```bash
# Запуск всех бенчмарков
cargo bench

# Запуск конкретного бенчмарка
cargo bench --bench crypto_bench
```

## Ресурсы

- [Rust Book](https://doc.rust-lang.org/book/)
- [Rust by Example](https://doc.rust-lang.org/rust-by-example/)
- [Tokio Documentation](https://docs.rs/tokio)
- [TDLib Documentation](https://core.telegram.org/tdlib/docs/)

## Вопросы?

Создайте issue или свяжитесь с мейнтейнерами.

---

Спасибо за ваш вклад! 🎉
